'use strict';
const React = require('react');

var filepicker = React.createClass({

    render: function () {
        return (
            <div />
        );
    }

});

module.exports = filepicker;
